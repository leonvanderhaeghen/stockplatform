// Copy the content of the original file and make the following changes:
// 1. Replace the price formatter with: valueFormatter: (params) => formatCurrency(params.value),
// 2. Replace the amount formatter with: valueFormatter: (params) => formatCurrency(params.value),
// 3. Make sure formatCurrency is imported from '../utils/formatters'
